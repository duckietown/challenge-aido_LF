challenge: aido5-LF-sim-validation

title: "LF 🚗 - Lane following  2 (simulation 👾, validation 🏋)"
tags: [visible, ml-validation, aido5, aido5-embodied, LF, v4, simulation]
description: |

  <table id="variations">
  <thead >
  <th><code>LF</code></td>
  <th><code></code></td>

  </thead>
  <tr>
  <td>Follow the lane</td>
  <td></td>
  </tr>
  <tr>
  <td>
  <img src="https://i.imgur.com/sGTigpp.jpg" style='width: 15em'>
  </td>
  <td class='explain'>
  <p>This is the basic lane following challenge.</p>

  <p>You must follow the lane using the outputs of camera and encoders.</p>

  <p>See the <a href="https://docs.duckietown.org/daffy/AIDO/out/lf.html">documentation</a> for more details.</p>
  </td>
  </tr>
  </table>

  *Note*: this is the _validation_ 🏋 challenge - you can see the output
  but these are not the scores that count.

protocol: aido2_db18_agent-z2


date-open: 2020-09-15T00:00Z
date-close: 2020-12-05T00:00Z

scoring: !include scoring.yaml

steps:
  LFv-sim:
    title: Simulation step
    description: ""
    features_required:
      compute_sims: 1 # computation machine

    timeout: 3600 # 1 hour
    evaluation_parameters:
      services:
        evaluator: !include comp_evaluator.yaml
        simulator: !include comp_simulator.yaml
        solution: !include comp_solution.yaml

        scenario_maker:
          environment:
            AIDONODE_DATA_IN: /fifos/scenario_maker-in
            AIDONODE_DATA_OUT: fifo:/fifos/scenario_maker-out
            AIDONODE_CONFIG: |
              maps:
              - ETHZ_autolab_technical_track
              scenarios_per_map: 4
              robots_npcs: []
              robots_pcs: [ego]
          image: ${AIDO_REGISTRY}/duckietown/challenge-aido_lf-scenario_maker:daffy-amd64


dependencies: {}
#  aido5-LF-sanity-sim-validation: !include dep-sanity.yaml


